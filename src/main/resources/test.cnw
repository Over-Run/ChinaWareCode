/*
    语法示例
    -> @author baka4n
*/

var a <- 1;//赋予动态值a 数值1
val b <- 2;//赋予固定值b 数值1
gobal c <- a+b; //创建全局固定变量c 并传递a+b的值
gobal c -> a,b; //固定变量c传递给a和b
b -> d,e;
var f <= 1 <> 2;//判断方式为 <=小于等于 => 大于等于 ==等于等于 <>不等于 &&和 || 或

if f {
    var g <- a;
    var h <- a;
}